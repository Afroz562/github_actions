name: Manaual Rollback with previous release Changes
on:
  push:
    branches:
      - master
  workflow_dispatch:
  
 jobs:
   latest-changes:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: docker://tiangolo/latest-changes:0.0.3
        with:     
      - name: Checkout
        uses: actions/checkout@v3
